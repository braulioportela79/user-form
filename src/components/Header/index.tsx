import { StyledTitle } from "styles/typograghy";

export const Header = () => {
  return (
    <StyledTitle fontSize="lg" fontWeight={500} tag="h1">
      Criação de Usuário
    </StyledTitle>
  );
};
